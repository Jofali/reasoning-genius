import React from 'react';

import './end.css';
import play from '../play';
function End(props) {
  if(props.name === "") {}

  if(props.name === "") {}

  if(props.name === "") {}
  return (
      <div className="begin">
        <div className="begin-content-wrap">
          <p>大结局</p>
        </div>
      </div>
  );
}

export default End;
